[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"
context = "."

[deploy]
startCommand = "/bin/sh -c 'uvicorn main:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"

[service]
name = "vibedigest-backend"
